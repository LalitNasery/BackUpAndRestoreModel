# BackUpAndRestoreModel
Java desktop tool built with Spring Boot and Swing for backing up and restoring MongoDB collections. Connects to any MongoDB instance, exports selected collections as BSON inside a ZIP file, and supports two restore modes — Drop &amp; Restore or Replace/Update by _id. Features a clean GUI with progress indicators.
